# LinkedIn-Queens-Puzzle-Solver
Python solver for the Queens puzzle on LinkedIn (or any other online platform). Uses pixel color detection to reconstruct the board, applies a recursive backtracking algorithm to compute a valid solution, and automatically fills the grid via GUI automation.
